CREATE TABLE `gcce`.`CalifAcademica` (
  `cod_matricula` INT NULL,
  `cod_titulo` INT NULL,
  `cod_profesor` INT NULL,
  `cod_alu` INT NULL,
  `cod_asignatura` INT NULL,
  `convocatoria` VARCHAR(45) NULL,
  `calif_num` INT NULL,
  `calif` VARCHAR(45) NULL,
  `presentado` VARCHAR(45) NULL);